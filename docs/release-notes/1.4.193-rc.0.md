# 跟进上游到 1.4.192

这一版主要是把上游 142 个提交合过来，同时保住这边自己的东西。

- 上游重构了 PR CI，顺手删掉了自建中继的检查任务；已经加回来，并且给了它独立的
  触发条件，中继测试挂了仍然会拦住合并。
- 手机端的多语言包装在合上游的新网络诊断功能时丢了 26 处，已补回。
- 工作区名字池里的 `Orca` 是虎鲸，和 `Manta` 是两个物种。之前的品牌替换把它俩
  合成了一个，池子里少了一个可用名字；已按上游逐个位置还原。

上游本轮以修复和重构为主：终端分屏回车路由、工作区选择器按路径等价解析、
GitLab 议题分页、远程 HTML 预览，以及一批降低文件行数的拆分。
