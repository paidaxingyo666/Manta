# 会话视图冻结的修复,这次装对了地方

rc.0 的修复没生效:守卫只装在了兜底路径上,而冻结时走的是另一条。

滚动后的旧文件不会被删除,只是不再增长——所以记录里那个路径依然存在、依然能
打开,首次绑定就成功了,兜底路径根本不会执行。也就是说守卫恰好缺席在它被写出来
要应付的那个场景里。桌面端和手机端读的是同一份订阅,所以两边一起停住。

在运行中的应用上确认过:会话绑着一个已静默十六分钟的文件,而对话正写入另一个。
修好后用真实文件验证,订阅确实从死文件切到了活文件并拿到最新内容。
