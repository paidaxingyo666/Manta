# 同步上游并修好一批改名遗留

这一版把上游 134 个提交并了进来，并修掉品牌替换时两边拼写不一致导致的几处失效。

- **agent 环境变量修好了**。写入和读取用了两套名字，OpenCode / Pi / MiMo 等 agent 的
  工作区环境注入一直是空的。
- **技能删除的暂存目录**建出来后匹配不上自己的规则，残留清不掉。
- **手机端新建工作区**时 hook 信任状态恒为空，每次都要重新确认。
- **浏览器抓取**的清理分支走不到，旧的覆盖层不会被移除。
- **更新检查**在已是最新版时不再绕到会 404 的地址。
- 界面上的「提交功能需求」之前会跳到上游仓库，现已指向本仓库。
